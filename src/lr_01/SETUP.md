# Setup

## Init project

```sh
$ npm init      # Initialize in a dialogue mode
$ npm init -y   # Initialize with default values
```

## Install server
install local server

## Install testing software
install -D jest for unit testing

install -D selenium for e2e testing
